# InterviewQuest AI — Frontend

React + Vite app for the interview UI.

## Local development

```sh
npm install
cp .env.example .env   # set VITE_API_URL if the backend isn't on localhost:8000
npm run dev
```

See `../docs/DEPLOYMENT.md` for how this is deployed alongside the backend.
